# Themes & Settings

The Palette toolbar button switches color themes (including High Contrast) — every window in the
suite shares the same theme. Settings tab: Auto-Save Enabled/Interval, a global Save hotkey, and
theme choice.

## Performance

Zero runtime cost, across the whole suite. Every product is an Editor-only assembly that Unity
strips from your build automatically.
